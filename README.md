# numerator-projects

Хранилище проектов нумератора схем (файлы `.json`).

## Что здесь
- Файлы проектов нумератора в формате `.json`.

## Как сохранять изменения
Из папки `J:\numerator-projects` выполнить:

```
git add .
git commit -m "описание изменений"
git push
```
